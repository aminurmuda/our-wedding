


function Closing({ onClick }) {

    return (
        <div className="center fullheight">
            <button className="action-button" onClick={onClick('home')}>Buka Undangan</button>
        </div>
    )
}

export default Closing